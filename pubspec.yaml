name: remove_bg
description: 传影--所见即所得

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
  get: ^4.1.4 
  get_server: ^1.1.0
  process_run: ^0.12.1
  web_socket_channel: ^2.1.0

  oktoast: ^3.0.0
  scan: ^1.4.3
  qr_flutter: ^4.0.0
  permission_handler: ^8.0.1

  simple_animations: ^3.1.1
  flutter_math_fork: ^0.3.3+1

  image: ^3.0.2
  camera: ^0.8.1+2
  path_provider: ^2.0.2
  flutter_image_compress: ^1.0.0

  auto_input:
    path: packages/auto_input/



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  flutter_launcher_icons: ^0.9.0

flutter:
  uses-material-design: true

  assets:
    - assets/

flutter_icons:
  android: true
  ios: true
  image_path: "logo.png"

